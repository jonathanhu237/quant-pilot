services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: quant_pilot
      POSTGRES_PASSWORD: quant_pilot
      POSTGRES_DB: quant_pilot
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
